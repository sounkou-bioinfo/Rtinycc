# Locate the TinyCC executable

Returns the platform-specific `tcc` binary path (or `tcc.exe` on
Windows), preferring the bundled installation.

## Usage

``` r
tcc_path()
```

## Value

A character scalar path.
