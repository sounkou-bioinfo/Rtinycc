# Create a new FFI compilation context

Initialize a Bun-style FFI context for API-mode compilation. This is the
entry point for the modern FFI API.

## Usage

``` r
tcc_ffi()
```

## Value

A tcc_ffi object with chaining support

## Examples

``` r
ffi <- tcc_ffi()
```
