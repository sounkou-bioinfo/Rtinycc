# Whether the async callback queue is drained

Convenience helper that returns \codeTRUE when no async callback is
currently queued.

## Usage

``` r
tcc_callback_async_is_drained()
```

## Value

Logical scalar
