% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffi.R
\name{tcc_link}
\alias{tcc_link}
\title{Link an external shared library with Bun-style FFI bindings}
\usage{
tcc_link(
  path,
  symbols,
  headers = NULL,
  libs = character(0),
  lib_paths = character(0),
  include_paths = character(0),
  verbose = FALSE
)
}
\arguments{
\item{path}{Path to the shared library (e.g., "libsqlite3.so")}

\item{symbols}{Named list of symbol definitions with:
\itemize{
\item args: List of FFI types for arguments
\item returns: FFI type for return value
}}

\item{headers}{Optional C headers to include}

\item{libs}{Library names to link (e.g., "sqlite3")}

\item{lib_paths}{Additional library search paths}

\item{include_paths}{Additional include search paths}

\item{verbose}{Print debug information}
}
\value{
A tcc_compiled object with callable functions
}
\description{
Link a system library (like libsqlite3.so) and generate type-safe
wrappers automatically using TinyCC JIT compilation (API mode).
Unlike dlopen(), this uses TinyCC to compile bindings that handle
type conversion between R and C automatically.
}
\examples{
\dontrun{
# Link SQLite with type-safe bindings
sqlite <- tcc_link(
  "libsqlite3.so",
  symbols = list(
    sqlite3_libversion = list(args = list(), returns = "cstring"),
    sqlite3_open = list(args = list("cstring", "ptr"), returns = "i32")
  ),
  libs = "sqlite3"
)

# Call directly - type conversion happens automatically
sqlite$sqlite3_libversion()
}
}
