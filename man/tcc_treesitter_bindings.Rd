% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treesitter_helpers.R
\name{tcc_treesitter_bindings}
\alias{tcc_treesitter_bindings}
\title{Generate bindings from a header}
\usage{
tcc_treesitter_bindings(
  header,
  mapper = tcc_map_c_type_to_ffi,
  ffi = NULL,
  functions = TRUE,
  structs = FALSE,
  unions = FALSE,
  enums = FALSE,
  globals = FALSE,
  bitfield_type = "u8",
  include_bitfields = TRUE
)
}
\arguments{
\item{header}{Character scalar containing C declarations.}

\item{mapper}{Function to map C types to FFI types.}

\item{ffi}{Optional \code{tcc_ffi} object. When provided, returns an updated
FFI object with generated bindings.}

\item{functions}{Logical; generate \code{tcc_bind()} specs for functions.}

\item{structs}{Logical; generate \code{tcc_struct()} helpers.}

\item{unions}{Logical; generate \code{tcc_union()} helpers.}

\item{enums}{Logical; generate \code{tcc_enum()} helpers.}

\item{globals}{Logical; generate \code{tcc_global()} getters/setters.}

\item{bitfield_type}{FFI type to use for bitfields.}

\item{include_bitfields}{Whether to include bitfields.}
}
\value{
Named list suitable for \code{tcc_bind()} when \code{ffi} is NULL, otherwise
an updated \code{tcc_ffi} object.
}
\description{
Generate bindings from a header
}
\examples{
\dontrun{
header <- "double sqrt(double x);"
symbols <- tcc_treesitter_bindings(header)
}
}
