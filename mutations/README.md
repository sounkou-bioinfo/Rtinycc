Mutation-testing fixtures and saved mutants live here.

They are excluded from package builds via `.Rbuildignore`, but kept in-tree so
mutation results can be inspected alongside the source they target.
